import { Layout } from 'antd';

const { Content } = Layout

export default function ListDevices() {
    return (
        <Layout>
            <Content>
                ListDevices
            </Content>
        </Layout>
    )
}